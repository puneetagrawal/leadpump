
						<p>
							<label></label>
							<% if DiscountsOnPeriod.all %>
							<% DiscountsOnPeriod.all.each do |dp| %>
							<label class="checkboxCon">
								<%= radio_button_tag 'planType', "#{dp.id}", :checked => "#{dp.id}" > "1" ?  false : true, :class => "plant #{dp.periodType}"%>
								<strong>Pay 
									<%= "#{dp.periodType}"%>
									<%if dp.discountPercentage > 0%>
									(save <%="#{dp.discountPercentage}" %>%)
									<%end%>
								</strong>
							</label>
							<% end %>
							<% end %>
						</p>
						<p>
							<label></label>
							<label class="checkboxCon">
								<input type="radio" name="paymentOptionRadio" checked="checked" class="creditCard"> <strong>Credit card</strong>
							</label>
							<label class="checkboxCon">
								<input type="radio" name="paymentOptionRadio" class="coupon"><strong>Coupon</strong>
							</label>
						</p>