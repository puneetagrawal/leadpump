<div class="container">
    <div class="row-fluid">
        <div class="span12">
           <div class="border">
                <table class="table table-striped">
                    <thead>
                    <tr><td>Name</td><td>Email</td><td>Plan Type</td><td>Plan Name</td><td>Payment</td><td># of Users</td></tr>
                    </thead>
                     	<% @users.each do |user| %>
                     	<% if user.subscription.present? %>
                    <tbody>
                    <tr> <td><%= user.name %></td> 
                  		 <td><%= user.email %></td>
                    	 <td><%= user.subscription.plan_type %> </td>
                         <td>  </td>
                         <td> <%= user.subscription.payment %> </td>
                         <td> <%= user.subscription.users_count %> </td>
                     </tr>
                    </tbody>
                    <% end %>
                    <% end %>
                </table>
            </div>
           <!--  <div class="row-fluid">
                <div class="span4 pull-left mt10">Showing 1 to 3 of 3 entries</div>
                <div class="span3 pull-right">
                    <ul class="pager">
                        <li class="previous">
                            <a class="link" href="#">&larr; Previous</a>
                        </li>
                        <li class="next">
                            <a class="link" href="#">Next &rarr;</a>
                        </li>
                    </ul></div>
            </div> -->
        </div>
    </div>
</div>

