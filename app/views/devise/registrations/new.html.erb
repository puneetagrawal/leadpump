<div class="container">
	<div class="row-fluid">
		<div class="span12 signup_heading">
			<h3>Thanks for choosing Buddy Referrer</h3>
			<h4>You're just 60 seconds away from your new account.</h4>
			<div class="signupCon">
				<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => {:class => "form form_block test", }) do |f| %>

				<div class="row-fluid">
		`			<span class="span7">
						<%= devise_error_messages! %>
						<div class="errors" id="stripe_error"></div>
						<%= render :template => "devise/registrations/_signupUserFields", :locals => { :f => f} %>	
						
						<%= f.fields_for :addresses, Address.new do |adr| %>
						<%= render :template => "common_templates/_addressFields", :locals => { :adr => adr} %>	
						<% end %>

						<h2><span class="number">3</span>Your billing information</h2>
						<%= render :template => "common_templates/_labelsForDiscounts", :locals => { :f => f} %>	
						<%= f.fields_for :subscriptions, Subscription.new do |sub| %>
						<%= render :template => "devise/registrations/_creditCardDetails", :locals => { :sub => sub} %>	
						<%end%>
						<span id="couponDiv" style="display:none">
							<p><%= label_tag :couponCode, "Coupon Code" %><%= text_field_tag :couponCode, nil, name: nil %></p>	
						</span>
						<div class="clear"></div>

					</span>
					<%= render :template => "devise/registrations/_signUpRightContent" %>		
						<div class="row-fluid">
							<div class="span12 mt20">
								<div class="span9">
									<p></p>
								</div>
							</div>
						</div>
						<div class="row-fluid">
							<div class="span12 acceptTerms">
								<label class="pull-left"><input type="checkbox" name="acceptConditions">I have read and accept the <a class="link" href="#">Terms and Conditions</a></label>
								<span class="submitSignUpForm">
									<a class="btn green pull-right" href="" id="signupBtn">Create My Account</a>
								</span>
							</div>
						</div>
						<% end %>
					</div>
				</div>

			</div>
		</div>

		<script>
		$(document).ready(function(){
			plan = "#{@plan.price}";	
			if($("input:radio[name='locationType']:checked").attr('class')  == 'multipleLocation'){
				$('#location_type').removeClass('hide')
			}
		})			
		</script>