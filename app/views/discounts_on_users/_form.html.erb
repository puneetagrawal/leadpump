<%= form_for(@discounts_on_user) do |f| %>
  <% if @discounts_on_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discounts_on_user.errors.count, "error") %> prohibited this discounts_on_user from being saved:</h2>

      <ul>
      <% @discounts_on_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userRanges %><br />
    <%= f.text_field :userRanges %>
  </div>
  <div class="field">
    <%= f.label :discountPercentage %><br />
    <%= f.number_field :discountPercentage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
