<h2><%=current_user.name.humanize%></h2>
<div class="row-fluid">
    <div class="span2">
        <div class="pull-left">
            <%if @picture_user.present?%>
                <%= image_tag @picture_user.avatar.url(:medium), :class=>"img-polaroid", :style=>"width:100%;height:suto;" %>
            <%else%>
                <img src="/assets/company-logo.png" class="img-polaroid">
            <%end%>
        </div>
    </div>
</div>


