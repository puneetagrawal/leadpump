<h2><%=current_user.name.humanize%></h2>
<div class="row-fluid">
    <div class="span2">
        <div class="pull-left">
            <%if current_user.picture.present?%>
                <%= image_tag current_user.picture.company_logo.url(:medium), :class=>"img-polaroid"%>
            <%else%>
                <img src="/assets/company-logo.png" class="img-polaroid">
            <%end%>
        </div>
    </div>
</div>


